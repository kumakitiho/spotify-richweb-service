(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const mobile=matchMedia('(max-width: 860px), (pointer: coarse)').matches||/iP(hone|od|ad)|Android/i.test(navigator.userAgent);const $=(q,r=document)=>r.querySelector(q),$$=(q,r=document)=>[...r.querySelectorAll(q)];const state={x:.5,y:.5,tx:.5,ty:.5,score:0,records:0,mood:'--',title:'Record Dive',overdrive:true,covers:[],t:0};function readStats(){state.score=Number(($('#hero-score')?.textContent||'0').replace(/\D/g,''))||0;state.records=Number(($('#metric-records')?.textContent||'0').replace(/\D/g,''))||0;state.mood=$('#metric-mood')?.textContent||'--';state.title=$('#now-title')?.textContent||'Record Dive';$('#hud-score')&&($('#hud-score').textContent=String(state.score).padStart(2,'0'));$('#hud-records')&&($('#hud-records').textContent=String(state.records).padStart(2,'0'));$('#hud-mood')&&($('#hud-mood').textContent=state.mood.slice(0,4));const name=state.mood==='CITY'?'Midnight Archive Room':state.mood==='ANTHEM'?'Anthem Mastering Room':state.mood==='DEEP'?'Deep Crate Observatory':state.mood==='ROCK'?'Distortion Memory Room':'Hybrid Listening Atelier';$('#dna-title')&&($('#dna-title').textContent=name);$('#dna-copy')&&($('#dna-copy').textContent=`「${state.title}」を中心に、${state.mood} の傾向を表示中。DIVE SCORE ${state.score} を元にListening DNAを更新しています。`)}function dnaHero(){const host=$('.hero-copy');if(!host||$('.dna-chamber'))return;const d=document.createElement('section');d.className='dna-chamber';d.innerHTML='<div class="dna-orbit"><span style="--a:0deg;--r:52px"></span><span style="--a:72deg;--r:55px"></span><span style="--a:144deg;--r:48px"></span><span style="--a:216deg;--r:58px"></span><span style="--a:288deg;--r:50px"></span></div><div class="dna-copy"><small>LISTENING DNA ENGINE</small><strong id="dna-title">解析待機中</strong><p id="dna-copy">トップ曲、ジャンル、人気度、棚の選択状態を合成して、音楽の“地層”を可視化します。</p></div>';($('#status-panel')||host).after(d)}function ribbon(){const stage=$('.shelf-stage');if(stage&&!$('.cinematic-ribbon',stage)){const r=document.createElement('div');r.className='cinematic-ribbon';r.innerHTML='<span>SPATIAL CRATE RENDER</span><strong>CSS 3D / SAFE RENDER MODE</strong>';stage.prepend(r)}}function boot(){const b=document.createElement('div');b.className='boot-sequence';b.innerHTML='<div class="boot-card"><small>RECORD DIVE SYSTEM</small><h2>Premium Room<br>Renderer</h2><div class="boot-line"><span>Render profile</span><b>'+(mobile?'mobile safe':'desktop rich')+'</b></div><div class="boot-line"><span>Listening DNA graph</span><b>armed</b></div><div class="boot-line"><span>Spatial crate engine</span><b>ready</b></div></div>';document.body.appendChild(b);setTimeout(()=>b.classList.add('is-done'),mobile?650:1150);setTimeout(()=>b.remove(),mobile?1200:2200)}function hud(){if(mobile)return;const h=document.createElement('aside');h.className='tech-hud';h.innerHTML='<div class="tech-hud__top"><span class="tech-hud__label">PREMIUM RENDER PIPELINE</span><span class="tech-hud__signal"><i></i><i></i><i></i>LIVE</span></div><div class="tech-hud__grid"><div class="tech-hud__cell"><span>DIVE</span><strong id="hud-score">00</strong></div><div class="tech-hud__cell"><span>CRATE</span><strong id="hud-records">00</strong></div><div class="tech-hud__cell"><span>MOOD</span><strong id="hud-mood">--</strong></div></div>';document.body.appendChild(h);const top=$('.topbar-actions');if(top){const b=document.createElement('button');b.type='button';b.className='ghost-btn overdrive-toggle is-on';b.textContent='DIRECTOR';b.onclick=()=>{state.overdrive=!state.overdrive;b.classList.toggle('is-on',state.overdrive);const sh=$('#shader-canvas');if(sh)sh.style.opacity=state.overdrive?'.34':'.12'};top.prepend(b)}}function shader(){if(mobile||reduce)return;const c=document.createElement('canvas');c.id='shader-canvas';document.body.prepend(c);const gl=c.getContext('webgl',{alpha:true,antialias:false});if(!gl)return;const v='attribute vec2 p;void main(){gl_Position=vec4(p,0.,1.);}';const f='precision highp float;uniform vec2 r,m;uniform float t,s;float h(vec2 p){return fract(sin(dot(p,vec2(41.7,289.1)))*87453.2);}float n(vec2 p){vec2 i=floor(p),q=fract(p);float a=h(i),b=h(i+vec2(1,0)),c=h(i+vec2(0,1)),d=h(i+1.);q=q*q*(3.-2.*q);return mix(a,b,q.x)+(c-a)*q.y*(1.-q.x)+(d-b)*q.x*q.y;}void main(){vec2 uv=(gl_FragCoord.xy-.5*r)/min(r.x,r.y);vec2 mm=(m-.5)*1.4;float d=length(uv-mm*.25);float a=atan(uv.y,uv.x);float ring=sin(20.*d-t*1.2+s*.05);float grain=n(uv*5.+vec2(t*.04,-t*.03));float beam=smoothstep(.018,.0,abs(sin(a*4.+t*.25))*d);vec3 brass=vec3(.78,.58,.26),moss=vec3(.34,.46,.32),ivory=vec3(.86,.78,.62);vec3 col=mix(moss,brass,grain*.72+.2*ring);col=mix(col,ivory,beam*.55);float alpha=(.10+.16*smoothstep(.64,1.25,grain)+.08*smoothstep(.86,1.,ring))*smoothstep(1.4,.06,d)+beam*.08;gl_FragColor=vec4(col,alpha);}';function sh(type,src){const x=gl.createShader(type);gl.shaderSource(x,src);gl.compileShader(x);return x}const p=gl.createProgram();gl.attachShader(p,sh(gl.VERTEX_SHADER,v));gl.attachShader(p,sh(gl.FRAGMENT_SHADER,f));gl.linkProgram(p);const buf=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buf);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,1,-1,-1,1,1,1]),gl.STATIC_DRAW);const loc=gl.getAttribLocation(p,'p'),ur=gl.getUniformLocation(p,'r'),um=gl.getUniformLocation(p,'m'),ut=gl.getUniformLocation(p,'t'),us=gl.getUniformLocation(p,'s');function resize(){const d=Math.min(devicePixelRatio||1,1.5);c.width=innerWidth*d;c.height=innerHeight*d;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';gl.viewport(0,0,c.width,c.height)}addEventListener('resize',resize);resize();const start=performance.now();function draw(now){state.x+=(state.tx-state.x)*.055;state.y+=(state.ty-state.y)*.055;gl.useProgram(p);gl.enableVertexAttribArray(loc);gl.vertexAttribPointer(loc,2,gl.FLOAT,false,0,0);gl.uniform2f(ur,c.width,c.height);gl.uniform2f(um,state.x,state.y);gl.uniform1f(ut,(now-start)/1000);gl.uniform1f(us,state.score);gl.drawArrays(gl.TRIANGLE_STRIP,0,4);requestAnimationFrame(draw)}draw(start)}function cursor(){if(mobile)return;const c=document.createElement('div');c.className='fx-cursor';document.body.appendChild(c);addEventListener('pointermove',e=>{state.tx=e.clientX/Math.max(1,innerWidth);state.ty=e.clientY/Math.max(1,innerHeight);c.style.transform=`translate(${e.clientX}px,${e.clientY}px) translate(-50%,-50%)`},{passive:true});document.addEventListener('pointerover',e=>{if(e.target.closest('button,a,input,.record-card,.tunnel-record'))c.classList.add('is-active')});document.addEventListener('pointerout',e=>{if(e.target.closest('button,a,input,.record-card,.tunnel-record'))c.classList.remove('is-active')})}function room(){if(mobile||$('#premium-room'))return;const s=document.createElement('section');s.className='premium-room';s.id='premium-room';s.innerHTML='<div class="premium-room__grid"><div class="premium-room__copy"><p class="eyebrow">PRIVATE LISTENING ROOM</p><h2>Record room<br>as an instrument.</h2><p>棚をただ並べるのではなく、あなたのトップ曲を3D空間に配置。選択したレコード、DIVE SCORE、MOOD VECTORに合わせてDNAグラフと空間照明が同期します。</p><div class="director-panel"><div class="director-card"><span>ENGINE</span><strong>WebGL</strong></div><div class="director-card"><span>SPACE</span><strong>CSS 3D</strong></div><div class="director-card"><span>DNA</span><strong>Canvas</strong></div></div></div><div class="premium-room__stage"><canvas id="premium-dna-canvas"></canvas><div class="record-tunnel" id="record-tunnel"></div><div class="premium-room__rail"></div></div></div>';const anchor=$('.experience')||$('main');anchor.before(s)}function syncCovers(){if(mobile)return;const cards=$$('.record-card');state.covers=cards.slice(0,12).map((c,i)=>({id:c.dataset.id,img:$('.record-cover',c)?.src,title:$('.record-caption strong',c)?.textContent||`#${i+1}`})).filter(x=>x.img);const tunnel=$('#record-tunnel');if(!tunnel)return;tunnel.innerHTML='';const n=Math.max(state.covers.length,1);state.covers.forEach((r,i)=>{const b=document.createElement('button');b.className='tunnel-record';b.type='button';b.dataset.id=r.id;b.title=r.title;const a=(i/n)*Math.PI*2,rad=34+Math.sin(i*.9)*12;b.style.setProperty('--x',`${Math.cos(a)*rad}%`);b.style.setProperty('--y',`${Math.sin(a)*rad*.62}%`);b.style.setProperty('--z',`${-80-i*10}px`);b.style.setProperty('--ry',`${Math.cos(a)*-34}deg`);b.style.setProperty('--rx',`${Math.sin(a)*18}deg`);b.innerHTML=`<img loading="lazy" src="${r.img}" alt="${r.title}">`;b.onclick=()=>{$(`.record-card[data-id="${CSS.escape(r.id||'')}"]`)?.click();$('#experience')?.scrollIntoView({behavior:'smooth',block:'center'})};tunnel.appendChild(b)})}function dnaCanvas(){if(mobile||reduce)return;const c=$('#premium-dna-canvas');if(!c)return;const ctx=c.getContext('2d');let visible=true;new IntersectionObserver(([e])=>visible=e.isIntersecting).observe(c);function resize(){const r=c.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,1.5);c.width=Math.max(1,r.width*d);c.height=Math.max(1,r.height*d);ctx.setTransform(d,0,0,d,0,0)}new ResizeObserver(resize).observe(c);resize();function draw(){if(visible){const w=c.clientWidth,h=c.clientHeight,cx=w/2,cy=h/2;state.t+=.014;ctx.clearRect(0,0,w,h);ctx.strokeStyle='rgba(199,161,90,.18)';ctx.lineWidth=1;for(let r=52;r<Math.max(w,h);r+=42){ctx.beginPath();ctx.arc(cx,cy,r+Math.sin(state.t+r*.02)*4,0,Math.PI*2);ctx.stroke()}const points=10,base=70+state.score*.9;ctx.beginPath();for(let i=0;i<=points;i++){const a=i/points*Math.PI*2-Math.PI/2;const amp=base+Math.sin(state.t*2+i)*28+((i%3)*state.records);const x=cx+Math.cos(a)*amp,y=cy+Math.sin(a)*amp*.72;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();const g=ctx.createRadialGradient(cx,cy,10,cx,cy,Math.max(w,h)*.55);g.addColorStop(0,'rgba(243,234,215,.08)');g.addColorStop(1,'rgba(199,161,90,.16)');ctx.fillStyle=g;ctx.fill();ctx.strokeStyle='rgba(199,161,90,.72)';ctx.lineWidth=2;ctx.stroke();ctx.fillStyle='rgba(243,234,215,.82)';ctx.font='700 11px system-ui';ctx.fillText('LISTENING DNA FIELD',22,32);ctx.fillStyle='rgba(199,161,90,.9)';ctx.font='900 34px system-ui';ctx.fillText(String(state.score).padStart(2,'0'),22,70)}requestAnimationFrame(draw)}draw()}function magnetic(){if(mobile)return;document.addEventListener('pointermove',e=>{state.tx=e.clientX/Math.max(1,innerWidth);state.ty=e.clientY/Math.max(1,innerHeight);const stage=$('.premium-room__stage');if(stage){const x=(state.tx-.5)*12,y=(state.ty-.5)*-10;stage.style.transform=`rotateX(${y}deg) rotateY(${x}deg)`}}, {passive:true})}function observe(){const mo=new MutationObserver(()=>{readStats();syncCovers()});['#hero-score','#metric-records','#metric-mood','#now-title','#record-shelf'].map($).filter(Boolean).forEach(el=>mo.observe(el,{childList:true,subtree:true,characterData:true}));setInterval(()=>{readStats();syncCovers()},mobile?2500:1200)}function init(){document.documentElement.classList.toggle('mobile-safe',mobile);boot();dnaHero();ribbon();if(!mobile){shader();cursor();hud();room();dnaCanvas();magnetic()}observe();readStats();syncCovers()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();})();